const github = {
    baseURL: "https://api.github.com/graphql",
    username: "hoda-nikpour",
    headers: {
        "Content-Type": "application/json",
        Authorization: "bearer the key is removed because of github security rules"
}
}
export default github;